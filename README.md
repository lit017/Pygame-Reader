# Pygame-Reader
Spritz-like TXT reader written in Python

This will read the textfile "pygame.txt"
and display it on screen with single words to read.

the display duration of a word is based on its length so that smooth reading is ensured


# TO DO
- Speed control
- Pause/Continue
- Open File Prompt
